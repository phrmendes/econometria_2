if (!require("pacman")) install.packages("pacman")

pacman::p_load(
  tidyverse,
  data.table,
  styler,
  tabulator,
  fixest,
  vroom,
  knitr,
  Hmisc,
  pbapply,
  knitr,
  wooldridge,
  collapse,
  markdown,
  rmarkdown,
  car,
  olsrr,
  equatiomatic,
  flightsbr,
  plm,
  lmtest,
  systemfit,
  AER,
  plotROC,
  janitor,
  mice,
  margins,
  install = F
)
